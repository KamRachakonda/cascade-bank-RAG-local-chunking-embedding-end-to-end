# 00 — Overview: What This Course Teaches

## Concept

**Retrieval-Augmented Generation (RAG)** answers questions by combining two
things that neither a search engine nor a raw LLM does well alone:

1. **Retrieval** — find the small number of passages, out of a large private
   corpus, that are actually relevant to a question.
2. **Generation** — hand those passages to a language model and ask it to
   compose a fluent, *grounded* answer that cites where each claim came from.

The point of RAG is that the LLM never has to "know" your private documents.
It only has to read whatever the retrieval step hands it, in-context, at
answer time. That means:

- You can update the corpus (add a new PDF) without retraining or
  fine-tuning anything.
- The model can say "I don't know" when the corpus doesn't cover a question,
  instead of confidently making something up.
- You can point to the exact chunk of text that backs every sentence of the
  answer.

This project — **Cascade Bank**, a fictional bank invented purely for this
teaching demo — builds a complete, local-first RAG pipeline over synthetic
policy PDFs, SOPs, and CSVs. Every stage of the pipeline is a real,
readable Python module you can run, break, and inspect.

> **Cascade Bank does not exist.** All documents are synthetic (Faker +
> templated text), generated by `src/data_gen/`. See
> [`01-synthetic-data.md`](01-synthetic-data.md).

## The 8-stage pipeline

```
 ┌──────────────┐   ┌───────────────┐   ┌───────────────┐   ┌────────────────┐
 │ 1. GENERATE  │──▶│ 2. EXTRACT    │──▶│ 3. CHUNK      │──▶│ 4. EMBED       │
 │ synthetic    │   │ raw text from │   │ token-bounded │   │ local          │
 │ PDFs/SOPs/   │   │ PDFs & CSVs   │   │ splits with   │   │ sentence-      │
 │ CSVs (Faker) │   │ (pypdf/pandas)│   │ overlap       │   │ transformers   │
 └──────────────┘   └───────────────┘   └───────────────┘   └───────┬────────┘
                                                                     │
 ┌──────────────┐   ┌───────────────┐   ┌───────────────┐   ┌───────▼────────┐
 │ 8. UI        │◀──│ 7. GENERATE   │◀──│ 6. RERANK     │◀──│ 5. CHROMA      │
 │ Chainlit chat│   │ Groq    │   │ local cross-  │   │ persistent     │
 │ (retrieve/   │   │ (openai/gpt-oss-120b),   │   │ encoder       │   │ vector store   │
 │ rerank/answer│   │ cited answer  │   │ re-scores     │   │ (unified       │
 │ panels)      │   │ or fallback   │   │ shortlist     │   │ collection)    │
 └──────────────┘   └───────────────┘   └───────▲───────┘   └───────┬────────┘
                                                 │                   │
                                                 └── 6. SEARCH ◀─────┘
                                             query embedding + cosine
                                                 top-k lookup
```

Read left-to-right, top row then bottom row: **generate → extract → chunk →
embed → Chroma → search → rerank → generate (answer) → UI**. The first four
stages ("ingest-time") run once (or whenever the corpus changes); the last
four ("query-time") run on every user question.

## The local-vs-Groq split, and why

This project deliberately does **not** use Groq (or any hosted API)
for embeddings or reranking — only for the final answer-generation step.

| Stage | Where it runs | Model | Why |
|---|---|---|---|
| Embeddings | **Local** (CPU, sentence-transformers) | `BAAI/bge-small-en-v1.5` (384-dim) | Groq has no embeddings endpoint — it is a chat-completions proxy over hosted LLMs, not an embeddings provider. Even if it did, embedding every chunk of a corpus over a network API is slow and costs money per call; a small local bi-encoder does it in milliseconds on CPU, for free, offline, and deterministically (good for teaching and for the on-disk cache in `src/embed/cache.py`). |
| Reranking | **Local** (CPU, sentence-transformers `CrossEncoder`) | `cross-encoder/ms-marco-MiniLM-L-6-v2` | Cross-encoder rerankers score every (query, passage) pair jointly, which is much more accurate than bi-encoder cosine similarity but too slow to run over an entire corpus. Running it locally over just the retrieved shortlist (`cfg.search.top_k` candidates) is fast and needs no API key at all. |
| Answer generation | **Groq** | `openai/gpt-oss-120b` | This is the one place an actual large language model is required to turn retrieved chunks into fluent prose with citations. Groq gives access to many hosted models behind a single `GROQ_API_KEY` and an OpenAI-compatible API, so the whole demo needs **exactly one** external credential. |

So: **two local models, one API key.** This keeps ingestion (generation +
extraction + chunking + embedding + storage) entirely offline and free to
re-run as many times as you like, and isolates the only paid/networked call
to the single step where it's actually needed — synthesizing the final
answer. If `GROQ_API_KEY` isn't set, everything up through reranking
still works, and the UI/CLI fall back to a "retrieved-context-only" mode
(see [`08-answer-generation.md`](08-answer-generation.md)).

## Folder map

```
RAG/
├── config.yaml                     # all tunable knobs (chunking, models, top_k, etc.)
├── src/
│   ├── config.py                   # get_config(), require_groq_key()
│   ├── data_gen/                   # Stage 1: synthetic corpus generation
│   │   ├── common.py                (CascadePDF, disclaimers, Faker helpers)
│   │   ├── generate_pdfs.py         (policy/manual PDFs)
│   │   ├── generate_sops.py         (SOP-structured PDFs)
│   │   ├── generate_csvs.py         (5 operational CSVs)
│   │   └── generate_all.py          (runs all three)
│   ├── ingest/                      # Stages 2-3: extract + chunk
│   │   ├── extract.py               (extract_pdf, extract_csv, extract_file)
│   │   └── chunk.py                 (chunk_source_file, chunk_all)
│   ├── embed/                       # Stage 4: local embeddings + cache
│   │   ├── local_embedder.py        (embed_texts, embed_query)
│   │   └── cache.py                 (EmbeddingCache, chunk_hash)
│   ├── store/                       # Stage 5: ChromaDB
│   │   ├── chroma_client.py         (get_or_create_collection, upsert_chunks)
│   │   ├── ingest_to_chroma.py       (ingest_all — chunk+embed+upsert)
│   │   └── inspect.py                (stats, sample, overlap, 2D projection)
│   ├── search/                      # Stage 6: semantic search
│   │   └── semantic_search.py       (semantic_search)
│   ├── rerank/                      # Stage 6b: cross-encoder rerank
│   │   └── cross_encoder_rerank.py  (rerank, search_and_rerank)
│   ├── generate/                    # Stage 7: Groq answer generation
│   │   ├── groq_client.py     (chat_completion)
│   │   └── answer_synthesis.py      (synthesize_answer, answer_only_mode)
│   └── ui/
│       └── app.py                   # Stage 8: Chainlit chat UI
├── ingest.py                        # project-root convenience CLI
├── scripts/run_full_pipeline.py     # one-shot generate→ingest→inspect→project
└── docs/course/                     # <- you are here
```

## Quickstart: run the whole thing

From the project root (the folder containing `config.yaml`):

```powershell
# 1. Create and activate a virtual environment
python -m venv .venv
.venv\Scripts\Activate.ps1

# 2. Install dependencies
pip install -r requirements.txt

# 3. Set your Groq key (only needed for the final answer-generation step)
copy .env.example .env
notepad .env      # set GROQ_API_KEY=sk-or-...

# 4. Run the full pipeline: generate corpus -> ingest -> inspect -> project
python scripts/run_full_pipeline.py

# 5. Launch the chat UI
chainlit run src/ui/app.py
```

`scripts/run_full_pipeline.py` runs, in order: `generate_all()` (writes
synthetic PDFs/SOPs/CSVs to `data/raw/`), `ingest_all(reset=True)`
(chunk → embed → upsert into Chroma, wiping any existing collection first),
`print_stats()`, and `embedding_projection()` (writes a 2D scatter plot to
`docs/course/embedding_scatter.html` / `.png`). Pass `--skip-gen` to re-run
ingestion against whatever is already in `data/raw/` without regenerating
the corpus.

If you don't have an `GROQ_API_KEY` yet, everything through step 4
still works — the UI and CLI tools fall back to a no-LLM
"retrieved-context-only" mode (see [`08-answer-generation.md`](08-answer-generation.md)).

## Lesson index

| # | Lesson | Stage(s) covered |
|---|---|---|
| 01 | [Synthetic Data](01-synthetic-data.md) | Generate |
| 02 | [Chunking & Overlap](02-chunking-and-overlap.md) | Chunk |
| 03 | [Embeddings & Cache](03-embeddings-and-cache.md) | Embed |
| 04 | [Vector Store: ChromaDB](04-vector-store-chromadb.md) | Store |
| 05 | [Inspection & Visualization](05-inspection-and-viz.md) | Store (inspect) |
| 06 | [Semantic Search](06-semantic-search.md) | Search |
| 07 | [Reranking](07-reranking.md) | Rerank |
| 08 | [Answer Generation](08-answer-generation.md) | Generate (LLM) |
| 09 | [Chainlit UI](09-chainlit-ui.md) | UI |
| 10 | [Exercises](10-exercises.md) | All stages |

## Teaching note

Every lesson in this course maps directly to real files in this repo — no
hand-waving pseudocode. When a lesson says `path:func`, that function exists,
verbatim, at that path as of this writing. If you change the code, the
lessons may drift; treat mismatches as a signal to update the docs, not the
other way around.
